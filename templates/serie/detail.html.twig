{% extends 'base.html.twig' %}

{% block title %}Détails de la série{% endblock %}

{% block stylesheets %}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
{% endblock %}

{% block body %}
    <style>
        body {
            background-image: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.9)),
            url('{{ asset('uploads/backdrops/' ~ serie.backdrop) }}');
            background-size: cover;
            background-position: center;
            color: white;
        }
        .card {
            background-color: rgba(255, 255, 255, 0.9);
        }
    </style>

    <div class="container mt-5">
        <h2 class="mb-4 text-center text-light">🎬 Détails de la série</h2>

        <div class="card shadow-lg">
            <div class="row g-0">
                <div class="col-md-4">
                    <img src="{{ asset('uploads/posters/series/' ~ serie.poster) }}" class="img-fluid rounded-start" alt="Poster de {{ serie.name }}">
                </div>
                <div class="col-md-8">
                    <div class="card-body">
                        <h3 class="card-title">{{ serie.name }}</h3>
                        <p class="card-text"><strong>Description :</strong> {{ serie.overview }}</p>
                        <p class="card-text"><strong>Status :</strong> {{ serie.status }}</p>
                        <p class="card-text"><strong>Vote :</strong> ⭐ {{ serie.vote }}</p>
                        <p class="card-text"><strong>Popularité :</strong> 🔥 {{ serie.popularity }}</p>
                        <p class="card-text"><strong>Première diffusion :</strong> {{ serie.firstAirDate|date('d/m/Y') }}</p>
                        {% if serie.lastAirDate %}
                            <p class="card-text"><strong>Dernière diffusion :</strong> {{ serie.lastAirDate|date('d/m/Y') }}</p>
                        {% endif %}
                        <div class="mt-4">
                            <a href="{{ path('serie_update',{id:serie.id}) }}" class="btn btn-warning me-2">Modifier</a>
                            <a href="{{ path('serie_delete') }}, {{ id: serie.id, token : csrf_token('delete'~serie.id) }}" class="btn btn-danger" onclick="return confirm('Etes-vous sûr de vouloir supprimer cette série ?')">Supprimer</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="text-center mt-4">
            <a href="{{ path('_list') }}" class="btn btn-outline-light">⬅️ Retour au catalogue</a>
        </div>
    </div>
{% endblock %}
